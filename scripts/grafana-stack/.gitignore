# Generated environment file — contains real credentials, never commit this
.env

# pve-exporter config — contains Proxmox passwords
config/pve-exporter/pve.yml

# Docker Compose override — generated by install.sh
docker-compose.override.yml
