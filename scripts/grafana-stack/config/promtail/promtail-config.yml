server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:

  # ─── System logs (host) ───────────────────────────────────────────────────
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: varlogs
          host: monitoring-server
          __path__: /var/log/*.log

  # ─── FortiGate syslog UDP 1514 ────────────────────────────────────────────
  - job_name: fortigate_udp
    syslog:
      listen_address: 0.0.0.0:1514
      listen_protocol: udp
      idle_timeout: 60s
      label_structured_data: yes
      labels:
        job: fortigate
    relabel_configs:
      - source_labels: [__syslog_message_hostname]
        target_label: host
    pipeline_stages:
      - regex:
          expression: '.*type="(?P<type>[^"]*)".*subtype="(?P<subtype>[^"]*)".*level="(?P<level>[^"]*)".*action="(?P<action>[^"]*)".*'
      - labels:
          type:
          subtype:
          level:
          action:

  # ─── FortiGate syslog TCP 1515 ────────────────────────────────────────────
  - job_name: fortigate_tcp
    syslog:
      listen_address: 0.0.0.0:1515
      listen_protocol: tcp
      idle_timeout: 60s
      label_structured_data: yes
      labels:
        job: fortigate
    relabel_configs:
      - source_labels: [__syslog_message_hostname]
        target_label: host
    pipeline_stages:
      - regex:
          expression: '.*type="(?P<type>[^"]*)".*subtype="(?P<subtype>[^"]*)".*level="(?P<level>[^"]*)".*action="(?P<action>[^"]*)".*'
      - labels:
          type:
          subtype:
          level:
          action:
